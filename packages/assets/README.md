# Shared website assets

Original screenshots/map illustrations extracted from the supplied website bundle. Used by the
website build through shared/assets/ and stored once, rather than base64-embedded in every page.
Imported screenshots show browser designs, not compiled native iOS or Android applications.

No font files or downloaded company-logo binaries are included. The map preview's remote brand
references remain separately documented and disabled by default in the consolidated static site.
