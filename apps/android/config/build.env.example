# PUBLIC mobile compile-time config; not secrets. Pass using Gradle -P.
OPENFUEL_API_BASE_URL=https://YOUR_STAGING_WORKER.workers.dev/api/v1
OPENFUEL_DATA_MODE=demo
# Demo UI intentionally cannot turn into a live map by changing a key.
# Finish the real map and API adapter before adding a live data mode.
