/* SPDX-License-Identifier: AGPL-3.0-only; generated from pages.json. */
window.OPENFUEL_PAGES=[{"id":"start","title":"Get started","group":"Start here","description":"Open the real station map, install Android, or run the Expo project.","body":"<h2 id=\"try\">Try OpenFuel</h2><p>OpenFuel is an open-source fuel map with a working web app, native Android app, React Native Expo project and SwiftUI source. The shared Cloudflare database contains real OpenStreetMap station locations.</p><p><a class=\"button\" href=\"../preview/\">Open the app</a> <a class=\"button secondary\" href=\"../downloads/openfuel-android.apk\" download>Download Android APK</a></p><div class=\"notice\"><strong>Real stations, community pump prices</strong><p>Allow location to find stations near you, or search a Canadian city. Prices are blank until someone reports what they actually saw at the pump. Community reports are public and unverified. Station and price coverage may be incomplete.</p></div><h2 id=\"local\">Run locally</h2><div class=\"code\"><button class=\"copy\">Copy</button><pre><code>npm install\nnpm run dev</code></pre></div><p>The local development database stays on your computer. Follow the root README for station data import and mobile build commands.</p><h2 id=\"next\">Explore the project</h2><ul><li><a href=\"#hosting\">Cloudflare Workers and D1</a></li><li><a href=\"#android\">Android and Expo development</a></li><li><a href=\"#ios\">iPhone development</a></li><li><a href=\"#testing\">Verification and platform limits</a></li></ul>"},{"id":"repo","title":"Repository layout","group":"Start here","description":"One monorepo for web, Android, Expo, iOS and the Cloudflare API.","body":"<h2 id=\"tree\">The source tree</h2><div class=\"code\"><button class=\"copy\">Copy</button><pre><code>apps/web/          Website + real station web app\napps/docs/         Handbook UI and content\napps/android/      Kotlin / Jetpack Compose\napps/expo/         React Native / Expo Go\napps/ios/          SwiftUI + Foundation core + XcodeGen\nservices/live/     Current Cloudflare Worker + D1\nservices/edge/     Earlier Worker references\nservices/api/      Earlier FastAPI / SQLite reference\nservices/registry/ Review model and PostgreSQL draft\npackages/         Shared contracts, data and design assets\ntools/            Build, import and verification commands\n.github/workflows/ CI and native build jobs</code></pre></div><h2 id=\"canonical\">One source for each application</h2><p>Each platform keeps its own build system and shares the public station API. One change can update the API, website and mobile clients together. Do not create nested Git repositories in the apps.</p><p>The earlier synthetic fixtures and interface studies are retained as historical design and test resources. The live web route reads real station data and rejects a sample API response.</p>"},{"id":"website","title":"Website & documentation","group":"Build","description":"A real Leaflet map, browser location and Canadian city search.","body":"<h2 id=\"edit\">Edit the source</h2><p>The landing page is in <code>apps/web/</code>. The working map is in <code>apps/web/preview/</code>. Documentation content is <code>apps/docs/pages.json</code>; the build generates <code>pages.js</code>.</p><h2 id=\"build\">Build the site</h2><div class=\"code\"><button class=\"copy\">Copy</button><pre><code>npm run build\nnpm run dev</code></pre></div><p>The web app and API share an origin. A plain static server can render the shell but needs the API for city search and station lookup. Website <code>/</code>, map <code>/preview/</code> and handbook <code>/docs/</code> ship together.</p><h2 id=\"reference\">Use the map</h2><p>On entry, the browser asks for location. The map shows the returned device coordinates and accuracy. If location is denied or unavailable, search a Canadian city, enter latitude and longitude, or move the map and choose Search this area. The app never substitutes a fictional current location.</p><p>Leaflet 1.9.4 is bundled locally. Tiles load directly from OpenStreetMap for the viewed area with visible attribution and normal browser caching. No bulk download or offline tile feature is provided.</p><button class=\"button secondary\" data-open-reference>Open the station map</button>"},{"id":"android","title":"Android development","group":"Build","description":"Install the native APK or develop with React Native and Expo Go.","body":"<h2 id=\"install\">Install Android</h2><p><a class=\"button\" href=\"../downloads/openfuel-android.apk\" download>Download Android APK</a></p><p>This is a development APK, not a Play Store release. Android may ask you to allow installation from your browser. Allow location in the app, or use a manual area. Real station coordinates are shared with the web app; missing prices stay unknown until reported.</p><h2 id=\"expo\">Use Expo Go</h2><p>The <code>apps/expo/</code> project provides a React Native implementation for Expo Go. Follow its README to install dependencies, start Metro and open the project on your phone. Your phone must be able to reach the development server. Expo source and the compiled native Android APK are separate deliverables.</p><h2 id=\"native\">Native development</h2><p>The Kotlin / Jetpack Compose project lives in <code>apps/android/</code>. Use its Gradle wrapper and README for build and device commands. The public API URL is safe to ship in the app; account credentials and signing secrets are not.</p><h2 id=\"evidence\">Verification</h2><p>Check the root README and evidence files for the exact APK build and emulator runs. An emulator test does not replace testing location and navigation on your own phone.</p>"},{"id":"ios","title":"iOS development","group":"Build","description":"A SwiftUI map-first implementation replaces the old list home; actual Xcode build and screenshot review are still required.","body":"<h2 id=\"source\">Map-first SwiftUI</h2><p>MapHomeView, SampleMapView, StationRowView, FuelMenuView and PreviewModel implement the map, fixed list, Cards, 48-point navigation actions, saved places, settings and native sheets. OpenFuelCore provides shared sample rules and atomic local persistence. A map-only background asset is not a screenshot of the UI.</p><div class=\"code\"><button class=\"copy\">Copy</button><pre><code>python3 tools/generate_mobile.py --check\npython3 tools/project.py ios-core\n# On a Mac with Xcode + XcodeGen:\npython3 tools/project.py ios-build</code></pre></div><h2 id=\"build\">Real project generation</h2><p>This is not a pre-generated project: XcodeGen reads apps/ios/project.yml, including public xcconfig inputs and a real UI-test target. No signing credentials or admin API key is embedded. The map screens use the shared Cloudflare prototype API, with local persistence for cached prices and favourites. The older reference APIClient remains available separately.</p><h2 id=\"status\">Do not confuse core tests with an iPhone build</h2><p>The Swift core runs on Linux and the SwiftUI files passed a syntax-only parser. No Apple SDK type-check, simulator capture or physical-device run was possible here. A passing native screenshot comparison requires the actual native-review workflow and human-approved baselines.</p><p><a href=\"#parity\">Visual review process</a> · <a href=\"#environment\">Xcode configuration</a></p>"},{"id":"api","title":"API & contracts","group":"Build","description":"Nearby real stations, Canadian city search and public unverified reports.","body":"<h2 id=\"cloud\">Shared station API</h2><table><thead><tr><th>Endpoint</th><th>Use</th></tr></thead><tbody><tr><td><code>GET /api/v1/health</code></td><td>Service and database health</td></tr><tr><td><code>GET /api/v1/stations?lat=53.54&amp;lon=-113.49&amp;radius=10000&amp;fuel=regular</code></td><td>Nearby real stations; radius is metres</td></tr><tr><td><code>GET /api/v1/geocode?q=Edmonton</code></td><td>Canadian city search</td></tr><tr><td><code>POST /api/v1/reports</code></td><td>A price actually observed at a station</td></tr></tbody></table><p>Live responses identify <code>mode: live</code> and <code>is_demo: false</code>. Station IDs have the form <code>osm-node-123</code> or <code>osm-way-123</code>. Prices are nullable integer thousandths of CAD per litre: a value of <code>1499</code> means <code>$1.499/L</code>, displayed as <code>149.9 ¢/L</code>. This is a unit example, not a reported pump price.</p><h2 id=\"report\">Price reporting</h2><p>A request contains <code>station_id</code>, <code>fuel_type</code>, <code>price_milli</code>, a random <code>client_id</code> and a unique <code>request_id</code>. Reuse the same request ID when retrying an unconfirmed submission. Only show a report as accepted after the service confirms its ID, station, fuel and price. Rate limits and validation apply.</p><p>Reports are public, anonymous and unverified. Only submit prices actually checked at that station. Run integration tests against local D1; never seed made-up pump prices into the public database.</p><h2 id=\"reference\">Earlier architecture</h2><p>The FastAPI/SQLite service and PostgreSQL/PostGIS registry drafts remain as earlier reference work. They are separate from the deployed Cloudflare API.</p><p><a href=\"../handbook/LIVE_API.md\">Current live API contract</a> · <a href=\"../openapi.json\">OpenAPI JSON</a></p>"},{"id":"parity","title":"Native visual parity","group":"Build","description":"Historical design references and honest native verification boundaries.","body":"<h2 id=\"shared\">Platform behavior and design references</h2><p>The current web app, Kotlin Android app and Expo project use real station coordinates. The earlier six-station fixture and illustrated map remain historical design/test resources. The SwiftUI app still needs migration and verification against the real-station flow.</p><h2 id=\"capture\">Use evidence from each actual app</h2><p>Browser screenshots verify browser rendering. Native screenshots must come from the Android app or iOS app being evaluated. JavaScript export proves bundling, not Expo device behavior. Do not claim pixel parity or an iPhone build from shared source alone.</p><p>Review map interaction, location permission denial, missing-price stations, reporting, cache age, keyboard behavior and large text on the intended device.</p><p><a href=\"../handbook/BUILD_STATUS.md\">Current build status</a> · <a href=\"../handbook/NATIVE_PARITY.md\">Historical visual acceptance requirements</a></p>"},{"id":"state","title":"Station lifecycle","group":"Data & community","description":"Additions, corrections and closures stay separate from a raw vote count.","body":"<div class=\"notice\"><strong>Current prototype</strong><p>The working app uses Cloudflare Workers and D1 for real station locations and unverified community prices. PostgreSQL/PostGIS and Supabase material below describes the retained registry foundation, not a prerequisite for this prototype.</p></div><h2 id=\"policy\">What the included policy does</h2><p>The tested model lives in <code>services/registry/policy.py</code>. Three proposal-scoped attestations raise triage priority; they do not publish a station. New records need checked evidence and review. Permanent closure requires two distinct reviewer inputs.</p><div class=\"callout\"><strong>Reviewer strings are not authentication.</strong><p>The production service still needs actual authorization, source verification, coordinated-abuse resistance and prevention of self-review. The model is not a deployed moderation API.</p></div>\n<h2 id=\"states\">Preserve station identity</h2><p>A status change or reopening retains history and version checks. Old prices do not mean a station is closed. Nearby records are duplicate candidates, not automatic merges. Reviewed changes append public history; private reviewer/attester details are not exported.</p><h2 id=\"database\">PostgreSQL is the next data layer</h2><p>The included schema is an unexecuted PostgreSQL/PostGIS draft. It is not the database currently running under the reference API. Test the schema, transaction paths and permissions before wiring either native app to it.</p><p>Read <code>docs/reference/DATABASE_AND_STATION_LIFECYCLE.md</code> in the source for the detailed original proposal. Imported OSM data, brand artwork and source code have separate licence obligations.</p>"},{"id":"maps","title":"Map, assets & navigation","group":"Data & community","description":"Real OpenStreetMap tiles and stations, with coordinate-based navigation.","body":"<h2 id=\"real\">Geography</h2><p>OpenFuel imports Canadian fuel-station locations from OpenStreetMap. Records can have missing addresses, names or amenities. Their provenance and import date are recorded with the dataset. OpenStreetMap data is available under ODbL and city names from GeoNames under CC BY 4.0.</p><h2 id=\"tiles\">Interactive maps</h2><p>The browser uses bundled Leaflet 1.9.4 and standard OpenStreetMap tiles. Attribution stays visible on desktop and mobile. The browser sends its normal user agent and an origin referrer, respects HTTP caching, and requests only tiles for the viewed map. There is no area-download feature.</p><p><a href=\"https://operations.osmfoundation.org/policies/tiles/\">OpenStreetMap tile policy</a> · <a href=\"https://www.openstreetmap.org/copyright\">OSM licence</a> · <a href=\"https://www.geonames.org/\">GeoNames</a></p><h2 id=\"navigation\">Directions</h2><p>Google Maps and Apple Maps links contain the actual selected station coordinates. The destination app handles origin location and route calculation. Straight-line distances in OpenFuel are not road distances or journey times.</p><h2 id=\"historical\">Earlier design assets</h2><p>The repository retains fictional map artwork and sample fixture sets for the earlier design study. They are not used as live station locations or current prices. Third-party brand logos are not bundled into the new web map.</p>"},{"id":"testing","title":"Build evidence","group":"Operate","description":"Executed checks, explicit unexecuted platform gates, and logs—not inferred release readiness.","body":"<h2 id=\"run\">Root checks</h2><div class=\"code\"><button class=\"copy\">Copy</button><pre><code>python3 tools/project.py check --native-cores --web\nnpm test\npython3 tools/generate_mobile.py --check</code></pre></div><h2 id=\"evidence\">Executed output</h2><div id=\"evidence-table\"></div><h2 id=\"not-run\">Still to execute</h2><p>Xcode/SwiftUI compilation, iPhone simulator screenshots, real-device handoff and the retained PostgreSQL/pgTAP migration path. Android and Cloudflare prototype checks are recorded in the executed output above. A workflow file is not a successful run, and the real map requires actual imported station records, valid coordinates and a working API. Test location coordinates used in browser/emulator checks are explicitly injected for reproducibility, not the operator’s physical location.</p><p>The visual gate fails when approved real native screenshots are missing. <a href=\"#parity\">Native parity</a> · <a href=\"#migrations\">Database checks</a></p>"},{"id":"privacy","title":"Privacy & source boundaries","group":"Operate","description":"One-shot location permission, public reports and device-local saved areas.","body":"<h2 id=\"location\">Location</h2><p>The web app requests a one-shot device location on entry. Your browser owns the permission decision. It displays the returned coordinates and accuracy; denial leaves city search and map exploration available. Coordinates go to OpenFuel to query nearby stations. The application does not request background location or track journeys.</p><h2 id=\"network\">Network requests</h2><p>Cloudflare serves the website and public API. OpenStreetMap receives requests for viewed map tiles, including normal IP/browser metadata and the site origin. Canadian city search uses a GeoNames index through the OpenFuel API. Choosing directions opens your map provider with the selected station coordinates.</p><h2 id=\"storage\">Local data</h2><p>The browser stores up to four searched-area station snapshots, favourites, and an anonymous reporting identifier. Area keys round coordinates to three decimals. Last-returned device position is held in memory for this session. Clear browser data in the map’s Privacy panel.</p><p>When a connection drops, cached station details remain readable. Tiles are not downloaded for offline use. Unconfirmed report attempts keep an idempotency key; they are only retried when you choose to submit, never silently sent in the background.</p><h2 id=\"reports\">Public price reports</h2><p>A report contains the station ID, fuel grade, price and random identifiers. The service timestamps it. Reports are unverified public observations; no account is required. Report only the price you actually saw, without personal information.</p><p>Nearby-search URLs contain coordinates. Automatic Worker invocation logs and tracing are disabled. Hosting and map providers may still receive ordinary network metadata. <a href=\"../handbook/PRIVACY.md\">Complete data-flow notice</a></p>"},{"id":"hosting","title":"Cloudflare hosting","group":"Operate","description":"Cloudflare Workers, static assets and D1, without a paid fuel feed.","body":"<h2 id=\"stack\">A small hosting stack</h2><p>Cloudflare Workers serves the website and API, and D1 stores real station records and community reports. Canadian stations and city names are imported into the database so normal lookups do not depend on a paid geocoder or a runtime Overpass query.</p><p>OpenStreetMap’s community tile service has a best-effort usage policy and is separate from Cloudflare. At larger traffic volumes, choose an appropriate tile provider. No commercial live pump-price feed is connected. Cloudflare costs depend on the account’s plan and actual usage.</p><h2 id=\"local\">Local development</h2><div class=\"code\"><button class=\"copy\">Copy</button><pre><code>npm install\nnpm run dev</code></pre></div><p>Follow the root README for current migration and station-import commands. The local database is separate from hosted D1. Use local D1 for test price submissions.</p><h2 id=\"deploy\">Deploy a fork</h2><p>Set your own Worker name, account ID, D1 binding and domain in <code>wrangler.jsonc</code>. Credentials remain in your development environment. Build the mobile APK separately, then build the site to include it in the download folder. The source archive contains the monorepo, not private local credentials or databases.</p>"},{"id":"environment","title":"Environment variables","group":"Operate","description":"Public clients share an endpoint; the database binding stays in the Cloudflare Worker.","body":"<h2 id=\"public\">Client configuration</h2><p>The web app uses same-origin <code>/api/v1</code>. Its public configuration and source download contain no database passwords. Android compiles a public HTTPS API endpoint; iOS uses its public Xcode configuration. Any setting shipped to a browser or mobile binary is inspectable.</p><h2 id=\"runtime\">Worker and database</h2><p>The root Wrangler configuration binds D1 as <code>DB</code>. Cloudflare account authorization stays in your development environment, and D1 access runs only inside the Worker. Browsers and apps never receive an account token.</p><h2 id=\"local\">Local and remote state</h2><p><code>npm run dev</code> uses a local database. <code>npm run db:remote</code> applies migrations to the hosted prototype database. A successful local test does not change the hosted deployment.</p><h2 id=\"legacy\">Earlier configuration</h2><p>Supabase, PostgreSQL and migration-environment examples remain for the retained registry foundation. They are not required credentials for the current D1 prototype.</p>"},{"id":"migrations","title":"Database migrations","group":"Operate","description":"One versioned schema path; local checks, staged rollout and explicitly approved production changes.","body":"<div class=\"notice\"><strong>Current prototype</strong><p>The working app uses Cloudflare Workers and D1 for shared sample prices. PostgreSQL/PostGIS and Supabase material below describes the retained registry foundation, not a prerequisite for this prototype.</p></div><h2 id=\"canonical\">One source of schema truth</h2><p>Only supabase/migrations is active. The old registry SQL is historical provenance. Keep source imports, private evidence, accepted audit events and schema migrations distinct.</p><div class=\"code\"><button class=\"copy\">Copy</button><pre><code>supabase start\nsupabase migration new add_station_access_details\n# Edit the new migration; then rebuild LOCAL development only:\nsupabase db reset --local\nsupabase db lint --local --level warning\nsupabase test db</code></pre></div><h2 id=\"remote\">Guarded remote rollout</h2><div class=\"code\"><button class=\"copy\">Copy</button><pre><code>python3 tools/database.py plan --target staging\npython3 tools/database.py apply --target staging --confirm YOUR_EXACT_STAGING_PROJECT_REF\n# After review, same migration history to production with explicit confirmation.</code></pre></div><p>Protect the production GitHub Environment with required reviewers. Choose one migration deployer, never an automatic second integration. No migration runs inside a website build, phone launch or ordinary API request.</p><h2 id=\"compatible\">Maintain old installed clients</h2><p>Add first, backfill in bounded batches, update compatible servers/clients, then remove old fields after a support window. Test real restore points, private storage backups and RLS. No remote reset, automatic sample seed or destructive rollback shortcut.</p><p><strong>Execution status:</strong> SQL and 20 pgTAP checks are written but have not run against PostgreSQL here.</p><p><a href=\"../handbook/DATABASE.md\">Full migration and maintenance runbook</a></p>"},{"id":"licence","title":"Licence & contributions","group":"Project","description":"AGPL-3.0-only for first-party code; data and third-party rights remain separate.","body":"<h2 id=\"scope\">Project licence</h2><p>The requested first-party code and documentation licence is <strong>AGPL-3.0-only</strong>. The full unmodified text is in the root <code>LICENSE</code>. Real OpenStreetMap station data is ODbL. GeoNames city search data is CC BY 4.0. Leaflet is BSD-2-Clause. Original historical fictional fixtures/map retain CC0. Logos, dependency assets and imported datasets are not relicensed by our code licence.</p><p><a href=\"../LICENSE.txt\">Read the full AGPL licence</a> · <a href=\"../NOTICE.txt\">Read the notices</a></p>\n<h2 id=\"history\">Preserve the provenance</h2><p>Earlier uploaded copies used MPL for client code and AGPL-or-later for some services. Their original scope notes are retained. Those past grants to recipients are not retroactively revoked. This source merge claims no authority over third-party marks or other owners’ artwork.</p>\n<h2 id=\"contribute\">Contribute in one place</h2><p>Change the canonical component, update its tests and relevant documentation, and run the root checks. A PR can include website, native and contract changes without producing separate ZIP forks. Submit only work you may contribute under the project terms.</p><p><a class=\"button secondary\" href=\"../downloads/openfuel-source.zip\" download>Download the source repository</a></p>"},{"id":"github","title":"GitHub & release workflow","group":"Project","description":"One public-source monorepo with independent application builds.","body":"<h2 id=\"source\">One repository</h2><p>The project keeps web, native mobile, Expo, API, database migrations and documentation in one repository. Use the root README for the canonical GitHub URL and current release status. The site also offers a complete <a href=\"../downloads/openfuel-source.zip\" download>source archive</a>.</p><h2 id=\"contribute\">Contribute</h2><p>Fork the repository, work in the relevant app or service, and include the checks that exercise your change. Use local D1 for report tests; never fill the public database with test pump prices. Do not create nested Git repositories inside the apps.</p><h2 id=\"builds\">Builds and releases</h2><p>Native APK and iOS build workflows are separate from the web deployment. A workflow is not evidence of a successful run; check executed logs. Debug Android downloads are development builds and do not imply a Play Store release. iPhone signing and App Store distribution require a separate release process.</p><p>Before a public push, scan every Git ref and the generated source archive. The repository includes a pinned Gitleaks workflow with only exact test/type-annotation exceptions. <a href=\"../handbook/PUBLICATION_AUDIT.md\">Initial publication audit and its limits</a></p>"}];
